import { D3DragDirective } from './d3-drag.directive';

describe('D3DragDirective', () => {
  it('should create an instance', () => {
    const directive = new D3DragDirective();
    expect(directive).toBeTruthy();
  });
});
