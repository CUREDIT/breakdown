import { VisNetDirective } from './vis-net.directive';

describe('VisNetDirective', () => {
  it('should create an instance', () => {
    const directive = new VisNetDirective();
    expect(directive).toBeTruthy();
  });
});
